import 'package:flutter/material.dart';

class orgStatisticsPage extends StatelessWidget {
  const orgStatisticsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}