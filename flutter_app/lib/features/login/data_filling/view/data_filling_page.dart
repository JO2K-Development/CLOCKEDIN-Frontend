import 'package:flutter/material.dart';

class DataFillingPage extends StatelessWidget {
  const DataFillingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}